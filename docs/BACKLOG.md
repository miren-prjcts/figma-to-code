# Backlog

Status meanings: **Proposed** = discussed only; **Planned** = ticket written, execution not approved; **Active** = explicitly approved and in progress; **Review** = awaiting supervisor validation; **Done** = verified and integrated.

Governance: before proposing or scoping a new component or token, see the
[Design System Charter](DESIGN_SYSTEM_CHARTER.md) (reusable scope, component-selection principles,
token/state/accessibility standards), the [Component Lifecycle](COMPONENT_LIFECYCLE.md) (proposal
through release notes), and the [Figma / Code Parity Checklist](FIGMA_PARITY_CHECKLIST.md) (code
review before serialized Figma work).

## Next execution batch — Phase Plan (locked 2026-08-27)

Baseline: the independent DS gap audit conducted 2026-08-27 (Foundations pages `6:3`–`6:6`,
`Components` page `6:8`, and the full `packages/ui`/`packages/tokens` source). This table replaces
the old `DS v1.1`/`v1.2`/`v1.3` rows — pre-DSv2 placeholders for the same scope now covered by
Phases 1–3, retired from active planning here, not deleted from history (see git log for the
original wording).

Constraints locked in for this batch (2026-08-27): no Figma Code Connect (Figma Pro plan does not
include it — `FIGMA_PARITY_CHECKLIST.md`'s manual process and `DSV2-006` remain the only parity
safeguards); no paid tooling anywhere in the batch; publishing/versioning `@repo/ui` stays out of
scope until a full first product version is assembled, per `DESIGN_SYSTEM_CHARTER.md` §7.

Each phase closes with its own supervisor-review gate (`COMPONENT_LIFECYCLE.md` §6) before the next
phase's Figma work starts — same serialized discipline as DS v1/v2. Naming a phase here is planning,
not authorization; tickets and model assignments still require separate explicit approval per the
Golden workflow.

| Phase | Priority | Scope                                                                                                                                                                                                                                                                                                                                                | Depends on                                          |
| ----- | -------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------- |
| 0     | 1        | Foundation completion: motion tokens (`--duration-*`, `--easing-*`), z-index/layering scale, elevation scale (beyond the single `shadow-soft`), icon system formalized as a Figma foundation page (Phosphor as documented source, not ad hoc per-component). Modal gets its enter/exit transition as a same-phase follow-on once motion tokens land. | None — can start immediately                        |
| 1     | 2        | Universal form core: FormField, Select, Checkbox, Radio, Switch, Textarea, IconButton. Button hardened with `size=lg` and a destructive variant. Verification tooling stood up _alongside_, not after: `DSV2-006` Playwright visual-regression baseline, and `vitest-axe` wired into `packages/ui`'s existing Vitest suite.                          | Independent of Phase 0; can run in parallel         |
| 2     | 3        | Feedback/overlay layer: Alert, Toast, Tooltip, Popover, Skeleton, standalone Spinner, Dropdown Menu (also closes StatCard's currently-dangling overflow action).                                                                                                                                                                                     | Phase 0's motion/z-index/elevation tokens           |
| 3     | 4        | Navigation, structural, and remaining hardening: Tabs, Breadcrumbs, Pagination, Avatar, Divider, Progress bar, Empty-state pattern, layout primitives (Stack/Container/Grid); StatCard trend indicator; Badge sizes and dismissible variant; Modal `md`/`lg` sizes.                                                                                  | Phases 1–2 for shared patterns (icon slots, motion) |

Data Table, Tree View, Calendar, Command Menu, Charts, and the rest of the Charter §4-deferred list
are unaffected by this batch — see "Deferred until product-driven discovery" below.

## Completed

| Workstream                                                                     | Outcome                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| ------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| DS foundations                                                                 | Code-linked Figma token collections, Geist typography, Light/Dark aliases, effect styles, and foundation documentation pages                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| DS v1 component contracts: Button, Input, Badge, StatCard, Modal               | Code integrated (`903344a`, `67429f0`, `6a85142`); Figma parity achieved via DSV1-006 (below)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| DSV1-006: Repair DS v1 Figma component-set parity through a staged replacement | Five components staged, validated (metadata, screenshots, Light/Dark, live property toggles), and cut over to the canonical `Components` page (`6:8`) on 2026-08-26 after user visual-checkpoint approval. The legacy archive and staging pages were later purged with explicit user approval.                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| DSV1-007: Align pnpm runtime and build-script configuration                    | pnpm `9.15.4` enforced via `engines.pnpm`; invalid pnpm-11 `allowBuilds` placeholder removed; full install + verification suite (typecheck, 32/32 tests, lint, build) passed on 2026-08-26                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| DSV2-001: Semantic token contract and theme mapping                            | 10 new interaction/foundation tokens added to `packages/tokens`; Storybook's `shadow-soft` self-reference repaired; web/Storybook Tailwind mappings reconciled. 33/33 tests passing. Integrated 2026-08-26 (`b0d34f9`, `cc2d709`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| DSV2-002: Interaction and accessibility hardening                              | StatCard's overflow action given a 44px (`--size-target-min`) hit area; Button's loading spinner respects `prefers-reduced-motion`. No public API changes. Integrated 2026-08-26 (`357b618`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| DSV2-003: System documentation and governance                                  | Added `DESIGN_SYSTEM_CHARTER.md`, `COMPONENT_LIFECYCLE.md`, `FIGMA_PARITY_CHECKLIST.md`; documented the DSV2-001 token/state additions in `figma-tokens.md`. Integrated 2026-08-26 (`2173743`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| DSV2-004: Serialized Figma foundation parity                                   | All 9 typography text styles bound to the pre-existing `Typography` collection's size/line-height variables (they existed, correctly scoped and code-synced, but unbound). The 10 DSV2-001/002 interaction/foundation tokens added as verified Figma Variables. No component set changed. Integrated 2026-08-26 (`ffd4673`).                                                                                                                                                                                                                                                                                                                                                                                                                            |
| DSV2-007-C: Icon and component-state parity correction                         | Real Phosphor-sourced icon components for close/loading/overflow; Modal converted to a single code-aligned component with Boolean/Text properties; StatCard's action states rebuilt on the real overflow icon. Integrated 2026-08-26.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| DSV2-007-D: Button loading structure correction                                | Fixed a structural defect DSV2-007-C's completion claim missed: Button's `loading` boolean never hid the label, so the spinner rendered on top of it on every variant. `loading` converted to a 5th `state` variant (30 variants); `leadingIcon`/`trailingIcon` converted from arrow-glyph text to real tone-matched icon instances, closing a pre-existing visibility-binding gap on 18 leaves found in the process. Same day, revised per user direction to a leading spinner + visible "Loading…" text (original accessible name preserved via `sr-only`), touching `packages/ui/src/components/button.tsx` and its test alongside the Figma rebuild. See [`DSV2-007-D`](tickets/DSV2-007-D-loading-structure-correction.md). Integrated 2026-08-27. |
| DSV2-008: Motion, elevation, and layering token contract                       | Added theme-invariant motion tokens (`--duration-fast/base`, `--easing-decelerate/accelerate`); retired the single `--effect-shadow-soft` for a named `sm`/`md`/`lg` elevation scale (Light/Dark values, `lg` carrying forward the old value); expanded `--layer-*` with named near-term Phase 2 consumers. Modal gained a real enter/exit transition (open → entering → entered → exiting → closed state machine), respecting `prefers-reduced-motion`. Code-only, no Figma mutation. Integrated 2026-08-27.                                                                                                                                                                                                                                           |
| DSV2-011: FormField, Checkbox, Radio, Switch                                   | FormField wrapper (label/description/error/required, automatic `id`/`aria-describedby`/`aria-invalid` wiring, reusing Input's `isAriaInvalid` convention, now exported from `input.tsx`). Checkbox (checked/unchecked/indeterminate/disabled/invalid), Radio+RadioGroup (native same-`name` grouping for roving-tabindex/Arrow-key behavior), Switch (`role="switch"`) — all on the existing `--size-target-min` hit-area pattern and semantic tokens only. Integrated 2026-08-27.                                                                                                                                                                                                                                                                      |
| DSV2-012: Select, Textarea                                                     | Textarea matches Input's exact visual contract, opt-in `autoResize` (fixed 3-row default). Select is a styled native `<select>` wrapper — no custom listbox, per Charter §4's "single correct API" test. Both consume the real `FormField` from DSV2-011 (reconciled from an interim Storybook-only stand-in used while that ticket was still in flight). Integrated 2026-08-27.                                                                                                                                                                                                                                                                                                                                                                        |
| DSV2-013: IconButton, Button `size=lg` and `destructive` variant               | Button gained `lg` (`--size-control-lg`) and a solid `destructive` variant on a dedicated, contrast-checked token set (`--destructive`/`-hover`/`-pressed`/`-solid-foreground`, not a reuse of the soft status tokens). IconButton shares Button's variant/size/state system with a required `aria-label` enforced at the type level, and the same 44px hit-area-without-visual-resize pattern as StatCard's overflow action. Integrated 2026-08-27.                                                                                                                                                                                                                                                                                                    |
| DSV2-014: vitest-axe integration                                               | `jest-axe` (not `vitest-axe`, which is effectively unmaintained) wired into `packages/ui`'s Vitest setup via `expect.extend`; axe assertions added to all existing and Phase 1 component test files. Pattern documented in `DESIGN_SYSTEM_CHARTER.md` §6 for later phases to reuse. Integrated 2026-08-27.                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| DSV2-006: Playwright visual regression harness                                 | Self-hosted Storybook screenshot diffing (`@playwright/test`'s `toHaveScreenshot`), story list read from Storybook's own build manifest so new stories are picked up automatically. Baseline covers all Phase 0/1 stories in Light/Dark (156 screenshots). CI runs this job on `macos-latest` (not the rest of the suite's `ubuntu-latest`) to match the committed macOS baseline's platform — free either way since this repo is public. See `docs/VISUAL_REGRESSION.md`. Integrated 2026-08-27.                                                                                                                                                                                                                                                       |

## Deferred until product-driven discovery

- Data Table, Tree View, Calendar/Date Picker, Charts, Command Menu, Side Navigation, Breadcrumbs, rich text, file upload, and advanced combobox/multiselect remain outside the current execution batch.
- The first seven named families are committed roadmap scope for this reusable starter, after foundations and universal core work. Each still requires a focused discovery, plan, and execution approval cycle to avoid premature APIs.
- This is the working precedent for the component-selection test in
  [Design System Charter §4](DESIGN_SYSTEM_CHARTER.md#4--component-selection-principles): each of
  these families fails the "a single correct API exists" test today.
